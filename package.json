{
  "name": "arctic-esports-functions",
  "description": "Server-side backend for Arctic Esports — the only place allowed to confirm a payment really happened.",
  "engines": { "node": "20" },
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.1.0",
    "firebase-functions": "^5.0.0",
    "razorpay": "^2.9.4",
    "cors": "^2.8.5"
  },
  "private": true
}
